# Registro do dia — 23/02/2026

## O que implementamos no sistema
- Máscara no número WhatsApp da clínica com `+55` fixo no input.
- Normalização no backend para salvar/aplicar o número em formato consistente.
- Bloqueio de campos de integração quando a instância está conectada:
  - Número WhatsApp da clínica
  - URL da Evolution API
  - Nome da instância
  - Token da API
- Regra de backend para impedir alteração desses campos enquanto conectado (proteção além do front).
- Estilo visual de campos bloqueados com aparência mais escura para ficar evidente.
- Carregamento de estado inicial de conexão ao abrir a tela de configurações (evita estado local inconsistente após refresh).
- Automação para n8n no sistema (webhook e fluxo de integração), reduzindo dependência de comando manual no PowerShell.
- Campo de modo de webhook usando `Select` do shadcn (melhor UX e consistência visual).

## Arquivos principais alterados
- `demo-environment/web/src/app/actions/bot-settings-actions.ts`
- `demo-environment/web/src/app/admin/settings/_components/bot-settings-panel.tsx`

## Infra / n8n / Evolution (debug e operação)
- Ajustado comportamento de recebimento no n8n para testes e produção.
- Corrigido cenário onde evento não chegava no editor por URL/modo incorreto.
- Confirmado que, com o fluxo publicado, as execuções estão batendo em `Executions`.
- Confirmado envio manual via PowerShell para validar webhook.
- Ajuste importante na Evolution: `webhook_by_events=false` para permitir entrega ao endpoint esperado no cenário atual.

## Fluxo n8n (status atual)
- Mensagens já estão chegando no fluxo.
- Nó `Normalizar Entrada` configurado e validado.
- `IF 1` filtrando `messages.upsert` corretamente.
- Triagem de intenção funcionando nos testes realizados hoje.

## Pendências para amanhã
1. Continuar do próximo nó após `IF 1` no fluxo de atendimento.
2. Conectar triagem -> ação por intenção (`agendar`, `cancelar`, `humano`, `saudacao`) com respostas mais robustas.
3. Substituir pontos antigos do Cal.com pelo consumo do nosso sistema de agendamento.
4. Finalizar ciclo completo: receber mensagem -> decidir intenção -> consultar/salvar no sistema -> responder no WhatsApp.

## Observação operacional
- Esse registro foi salvo para facilitar replicação com clientes e continuidade do passo a passo técnico.
